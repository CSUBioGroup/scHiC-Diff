lightning:
  trainer:
    enable_progress_bar: false
    log_every_n_steps: 20
    max_epochs: 3000
    accelerator: gpu
    devices:
    - 0
  modelcheckpoint:
    params:
      every_n_epochs: 500
      save_top_k: -1
